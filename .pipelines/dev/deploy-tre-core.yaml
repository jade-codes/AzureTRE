trigger:
  branches:
    include:
      - main
  paths:
    include:
      - .pipelines/dev/deploy-tre-core.yaml
      - ./core/**

variables:
  - group: tre-core-variables-dev
  - group: tre-core-auth-secrets-dev
  - group: azure-environment

pool:
  vmImage: ubuntu-latest

jobs:
  - template: /.pipelines/jobs/deploy-tre-core.yaml
    parameters:
      LOCK_ENV: "dev"
