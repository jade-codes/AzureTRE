trigger:
  branches:
    include:
      - main
  paths:
    include:
      - .pipelines/dev/workspaces/build-publish-register-workspace-bundle-unrestricted.yaml
      - ./templates/workspaces/unrestricted/**
pool:
  vmImage: ubuntu-latest

variables:
  - group: tre-core-variables-dev
  - group: tre-core-auth-secrets-dev
  - group: azure-environment

jobs:
  - template: /.pipelines/jobs/build-publish-register-workspace-bundle.yaml
    parameters:
      WORKSPACE_BUNDLE: "unrestricted"
      LOCK_ENV: "dev"
