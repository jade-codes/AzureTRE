{{- if .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ include "tre.fullname" . }}-gateway-class
spec:
  controllerName: {{ .Values.gateway.controllerName }}
{{- end }}
